{
  "schemaVersion": 1,
  "id": "houz42.global-protect",
  "name": "GlobalProtect VPN",
  "version": "1.0.0",
  "author": "houz42",
  "description": "Shows GlobalProtect VPN tunnel status and connects/disconnects by gateway from a popup.",
  "kinds": ["bar-widget"],
  "entryPoints": {
    "barWidget": "Panel.qml"
  },
  "barWidget": {
    "displayName": "GlobalProtect VPN",
    "description": "GlobalProtect VPN tunnel status, switched from a popup instead of on click.",
    "category": "Network",
    "allowMultiple": false,
    "defaults": {
      "refreshIntervalSec": 15
    },
    "schema": [
      { "key": "refreshIntervalSec", "type": "integer", "label": "Refresh interval (seconds)", "min": 5, "max": 300, "step": 5, "defaultValue": 15 }
    ]
  }
}
